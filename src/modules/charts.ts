/**
 * Chart is the container of utilities of methods
 *
 * @class Charts
 */
class Charts {
  /**
   * scales the chart
   *
   * @returns {number} returns the aspect ratio
   * @memberof Charts
   */
  public scale(): number {
    return 56;
  }
}
export { Charts };
